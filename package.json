{
  "name": "eliteledger-chat-proxy",
  "version": "1.0.0",
  "description": "Proxy to forward site chat requests to Gemini securely (reads GEMINI_API_KEY from env)",
  "main": "server-proxy.js",
  "scripts": {
    "start": "node server-proxy.js",
    "dev": "nodemon server-proxy.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^6.8.0"
  },
  "engines": {
    "node": ">=18"
  }
}
